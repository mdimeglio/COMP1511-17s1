#include <stdio.h>

#define MAX_LINE 4096

int main(void) {
    char line[MAX_LINE];
    int  i;
    
    // Use fgets to read in from stdin
    // Count and print num characters (not including \n in the line)
    // Repeat until end of input stream
    return 0;
}
